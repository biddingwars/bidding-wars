{
  "name": "bidding-wars",
  "version": "1.0.0",
  "private": true,
  "description": "A country auction game. Population is points, money runs out.",
  "scripts": {
    "ios": "npx cap sync ios && npx cap open ios",
    "sync": "npx cap sync",
    "serve": "npx serve www -l 5173"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/haptics": "^6.0.0",
    "@capacitor/preferences": "^6.0.0",
    "@capacitor/splash-screen": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0",
    "@capacitor/keyboard": "^6.0.0",
    "three": "0.128.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
